class User
  attr_accessor :name, :age

  def initialize(name, age)
    @name = name
    @age = age
  end

  def profile
    "#{name}（#{age}歳）"
  end
end

user1 = User.new("ルビー", 25)
puts user1.profile
